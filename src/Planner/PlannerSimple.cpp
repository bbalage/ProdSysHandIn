#include "Planner.hpp"

Plan PlannerSimple::plan(const Model &model) const
{
    Plan plan;
    // plan.sch_matrix.resize(model.agents.size());
    // for (uint i = 0; i < model.cargos.size(); ++i)
    // {
    //     plan.sch_matrix[i % model.agents.size()].push_back(i);
    // }
    return plan;
}